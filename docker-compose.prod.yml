services:
  app:
    network_mode: "host"
    ports: !reset []
    environment:
      - KIOSK_EXIT_TOKEN=${KIOSK_EXIT_TOKEN:-pi-caster-kiosk-exit}

  proxy:
    network_mode: "host"
    ports: !reset []
    environment:
      - APP_HOST=127.0.0.1
